initial_conditions_file: init_cond.init
actions:
  - [Moments, {Every: 1}]
config:
  t_max: 3.1
  dim: {D1: [8,1]}
  dirs: [X]
integrator:
  Euler:
    dt: 0.001
symbols: |
  f(_a,*b) := b[a]
  u' := f(x,u)
  v' := (1;1)
  *u := u[_i]+1
  *v := (v[_i]+1;v[_i+1]+1)
